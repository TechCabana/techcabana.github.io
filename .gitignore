# Secrets — never commit. See CLAUDE.md §9.
.env
.env.*
!.env.example

# OS and editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Local tooling
node_modules/
*.log

# Impeccable design context — local only, never published.
# The repo is public and published straight from main, so these stay out of it.
PRODUCT.md
DESIGN.md
.impeccable/
